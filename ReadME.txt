
////////////// PRODUCT UPDATES /////////////////////

USE MARSHELL;
 
ALTER TABLE PRODUCT DROP COLUMN PRODUCTSTOCK;
ALTER TABLE PRODUCT DROP INDEX `PRODUCTCODE_2` ;
ALTER TABLE PRODUCT ADD COLUMN TYPE BIT(1) NOT NULL DEFAULT 0; 

alter table product drop active;
alter table product drop type; 
ALTER TABLE PRODUCT ADD COLUMN TYPE BIT NOT NULL DEFAULT 0;
ALTER TABLE PRODUCT ADD COLUMN ACTIVE BIT NOT NULL DEFAULT 1; 


//////////// CUSTOMERS /////////////////////////////

SELECT ID,CNAME,SRNAME,ifnull(IDNUMBER,'626-NICA') IDNUMBER,MOBILE,ifnull(LOCATION,'NIC') LOCATION,ifnull(NATIONALITY,'NIC') NATIONALITY,GENDER,BLKCUS,OW,CUR,active FROM marshell.customer;


USE MARSHELL;
ALTER TABLE CUSTOMER ADD COLUMN ACTIVE BIT NOT NULL DEFAULT 1; 
ALTER TABLE CUSTOMER DROP COLUMN ACTIVE ; 

 
///////////////// NEW INVOICE TABLES  //////////////////////////////
 
CREATE TABLE `marshell`.`invoice` (
  `ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `CUSTOMER` VARCHAR(150) NULL,
  `DATE` DATETIME NOT NULL DEFAULT Now(),
  `TOTAL` DECIMAL(9,2) NOT NULL DEFAULT 0.00,
  `STAT` BIT(1) NOT NULL DEFAULT 1,
  `OFF` DECIMAL(2,2) NOT NULL DEFAULT 0.00,
  `EXRATE` DECIMAL(9,2) NOT NULL DEFAULT 36.80,
  `NIO` BIT(1) NOT NULL DEFAULT 1,
  `ACTIVE` BIT(1) NOT NULL DEFAULT 1,
  PRIMARY KEY (`ID`),
  UNIQUE INDEX `ID_UNIQUE` (`ID` ASC) VISIBLE);

ALTER TABLE `marshell`.`invoice` 
ADD COLUMN `usrId` INT NOT NULL AFTER `ACTIVE`,
ADD INDEX `FK_usrid_idx` (`usrId` ASC) VISIBLE;
;
ALTER TABLE `marshell`.`invoice` 
ADD CONSTRAINT `FK_usrid`
  FOREIGN KEY (`usrId`)
  REFERENCES `marshell`.`users` (`ID`)
  ON DELETE NO ACTION
  ON UPDATE NO ACTION;

ALTER TABLE `marshell`.`invoice` 
ADD COLUMN `CusId` INT NOT NULL AFTER `CUSTOMER` ; 


ALTER TABLE `marshell`.`invoice` 
ADD COLUMN `Cashed` DECIMAL(9,2) NOT NULL  ; 


ALTER TABLE `marshell`.`invoice` 
ADD COLUMN `FACN` Text NOT NULL default 'N/A' after ID ; 
 

 ID,CUSTOMER,CusId,usrId,DATE,TOTAL,STAT,OFF,EXRATE,NIO,ACTIVE,FACN

///////////////// NEW INVOICE DETAILS TABLES ///////////////////////

CREATE TABLE IF NOT EXISTS INVOICEDETAILS
(
	ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT UNIQUE,
    INVID INT UNSIGNED NOT NULL,
    PID INT NOT NULL,
    QTY DECIMAL(9,2) NOT NULL DEFAULT 1,
    PRICE DECIMAL(9,2) NOT NULL DEFAULT 0.00,
    OFF DECIMAL(2,2) NOT NULL DEFAULT 0.00,
    DATE datetime NOT NULL DEFAULT Now(),
    CONSTRAINT FK_INV FOREIGN KEY(INVID) REFERENCES INVOICE(ID),
    CONSTRAINT FK_PID FOREIGN KEY(PID) REFERENCES PRODUCT(ID)
);


ALTER TABLE `marshell`.`invoicedetails` 
ADD COLUMN `Acitve` BIT(1) NOT NULL DEFAULT 1 AFTER `DATE`;




TODO


START CAJA WITH AMOUNT OF CASH...

AND THE END CLOSE AND SUM ALL.. MAKE FROM EACH CAJA..


CREATE TABLE CATEGORY
(
	ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    NAME VARCHAR(100) NOT NULL UNIQUE,
    ACTIVE BIT(1) NOT NULL DEFAULT 1
);

CREATE TABLE MONEY
(
	ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    NAME VARCHAR(100) NOT NULL UNIQUE,
    SIMBOL VARCHAR(4) NOT NULL UNIQUE,
    ACTIVE BIT(1) NOT NULL DEFAULT 1
);
 

DELIMITER //

CREATE PROCEDURE sp_Save (
    IN p_ID INT,
    IN p_UNAME VARCHAR(50),
    IN p_SRNAME VARCHAR(50),
    IN p_IDNUMBER VARCHAR(50),
    IN p_MOBILE VARCHAR(20),
    IN p_REGDATE DATETIME,
    IN p_NATIONALITY VARCHAR(10),
    IN p_GENDER VARCHAR(10),
    IN p_LOCATION VARCHAR(100),
    IN p_LOGIN VARCHAR(100),
    IN p_PASS TEXT,
    IN p_FIRED INT,
    IN p_ROLE VARCHAR(50)
)
BEGIN
    -- Check if the ID exists
    IF EXISTS (SELECT * FROM YourTableName WHERE ID = p_ID) THEN
        -- Update existing record
        UPDATE YourTableName
        SET UNAME = p_UNAME,
            SRNAME = p_SRNAME,
            IDNUMBER = p_IDNUMBER,
            MOBILE = p_MOBILE,
            REGDATE = p_REGDATE,
            NATIONALITY = p_NATIONALITY,
            GENDER = p_GENDER,
            LOCATION = p_LOCATION,
            LOGIN = p_LOGIN,
            PASS = p_PASS,
            FIRED = p_FIRED,
            ROLE = p_ROLE
        WHERE ID = p_ID;
    ELSE
        -- Insert new record
        INSERT INTO YourTableName (UNAME, SRNAME, IDNUMBER, MOBILE, REGDATE, NATIONALITY, GENDER, LOCATION, LOGIN, PASS, FIRED, ROLE)
        VALUES (p_UNAME, p_SRNAME, p_IDNUMBER, p_MOBILE, p_REGDATE, p_NATIONALITY, p_GENDER, p_LOCATION, p_LOGIN, p_PASS, p_FIRED, p_ROLE);
    END IF;
END //

DELIMITER ;


 
// *****************************************************************************************************
// ********************            UPDAETE VERSION AGOST 2024                ***************************
// *****************************************************************************************************

DROP TABLES FROM INVOICE AND INVOICE DETAILS  AND CREATE NEW ONE'S 
HERE IS THE SCRIPTS

//
//UPDATE FOR INVOICE TABLES
//

CREATE TABLE Invoice (
    InvoiceID INT AUTO_INCREMENT PRIMARY KEY,
    InvoiceNumber VARCHAR(50) NOT NULL,
    CustomerID INT,
    InvoiceDate DATETIME DEFAULT CURRENT_TIMESTAMP,
    TotalAmount DECIMAL(10, 2) NOT NULL,
    PaidAmount DECIMAL(10, 2),
    DueAmount DECIMAL(10, 2),
    Status VARCHAR(20) CHECK (Status IN ('Paid', 'Unpaid', 'Partially Paid')),
    PaymentMethod VARCHAR(50),
    Notes TEXT,
    CreatedAt DATETIME DEFAULT CURRENT_TIMESTAMP,
    UpdatedAt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (CustomerID) REFERENCES Customer(ID)
);

//
//UPDATE FOR INVOICE DETAILS TABLES
//

CREATE TABLE InvoiceDetails (
    InvoiceDetailID INT AUTO_INCREMENT PRIMARY KEY,
    InvoiceID INT NOT NULL,
    ProductID INT NOT NULL,
    Quantity INT NOT NULL,
    UnitPrice DECIMAL(10, 2) NOT NULL,
    Discount DECIMAL(10, 2),
    Total DECIMAL(10, 2) GENERATED ALWAYS AS (Quantity * UnitPrice - COALESCE(Discount, 0)) STORED,
    CreatedAt DATETIME DEFAULT CURRENT_TIMESTAMP,
    UpdatedAt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (InvoiceID) REFERENCES Invoice(InvoiceID),
    FOREIGN KEY (ProductID) REFERENCES Product(ID)
);

//
//UPDATE FOR CUSTOMERS TABLES
//
ALTER TABLE CUSTOMER ADD CreatedAt DATETIME DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE CUSTOMER ADD UpdatedAt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;


//
//UPDATE FOR PRODUCT TABLES
//
ALTER TABLE PRODUCT ADD CreatedAt DATETIME DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE PRODUCT ADD UpdatedAt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;



//
// ALTER TABLE TO ADD USERS
//
ALTER TABLE users
ADD COLUMN IsDeleted BOOLEAN DEFAULT FALSE;